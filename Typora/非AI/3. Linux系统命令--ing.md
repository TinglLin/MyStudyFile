# Linux 系统命令

### Linux主要目录速查表

| 目录路径 | 含义                                             |
| -------- | ------------------------------------------------ |
| /bin     | 二进制命令所在目录                               |
| /boot    | 系统引导程序所需要的文件目录,引导系统开机        |
| /etc     | 系统配置,启动程序                                |
| /home    | 普通用户目录,目录默认数据存放目录                |
| /lib     | 启动系统和运行命令所需的共享库文件和内核模块存放 |
| /opt     | 额外的应用软件包                                 |
| /proc    | 操作系统运行时,进程信息和内核信息存放在这里      |
| /root    | Linux超级权限用户root的家目录                    |
| /sbin    | 和管理系统相关的命令,超级管理员用                |
| /tmp     | 临时文件目录,这个目录被当作回收站使用            |
| /usr     | 用户或系统软件应用程序目录                       |
| /var     | 存放系统日志的目录                               |







### Mark 内容

### echo和tee的区别

```
[zhang@localhost ~]$ echo "123" > a.txt
[zhang@localhost ~]$ cat a.txt
123

[zhang@localhost ~]$ echo "123" | tee a.txt
123


用 echo 写系统文件，会报错：
sudo echo "xxx" > /etc/xxx
# 报错：Permission denied
因为 > 重定向 不受 sudo 保护
但 tee 就完全没问题：
echo "xxx" | sudo tee /etc/xxx
```

### 常用命令

```
# 查看是否安装了某个软件
dpkg -l | grep vim
# 更简单的判断
which vim

```



## Ubuntu 发行版

```
# 查看是否安装了某个软件
apt list --installed
```





## CentOS 发行版

| 命令                | 含义       | 解释             |
| ------------------- | ---------- | ---------------- |
| rpm -qa \| grep xxx | 看安装软件 | -q quit;  -a all |
| yum install -y xxx  | 装软件     |                  |
| yum remove -y xxx   | 删软件     |                  |
|                     |            |                  |

